#include <bits/stdc++.h>
using namespace std;

int main() {
    
    int cnt=0;
    while(true)
    {
        string str;
        std::cin>>str;
        if(str=="") break;
        cnt++;
    }
    cout<<cnt;
    return 0;
}